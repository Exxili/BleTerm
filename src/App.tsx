import "./App.css";
import MainLayout from "./layout/Main.layout";

/**
 * @component App
 * @description Root component that renders the main layout.
 * @returns {JSX.Element}
 */
const App = () => {
  return (
    <>
      <MainLayout />
    </>
  );
};

export default App;
